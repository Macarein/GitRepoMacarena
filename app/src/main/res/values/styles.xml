<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Base Style -->
    <style name="BaseMinAccesibilityElement">
        <item name="minWidth">48dp</item>
        <item name="minHeight">48dp</item>
    </style>

    <!-- Base Style TextInputLayout as TextInputEditText -->
    <style name="TextInputLayout" parent="Widget.MaterialComponents.TextInputLayout.OutlinedBox">
        <item name="android:layout_marginStart">40dp</item>
        <item name="android:layout_marginEnd">40dp</item>
        <item name="android:textColorHint">@color/blue</item>
        <item name="boxCornerRadiusTopEnd">8dp</item>
        <item name="boxCornerRadiusBottomStart">8dp</item>
        <item name="boxCornerRadiusBottomEnd">8dp</item>
        <item name="boxCornerRadiusTopStart">8dp</item>
        <item name="boxStrokeColor">@color/blue</item>
        <item name="expandedHintEnabled">true</item>
        <item name="hintAnimationEnabled">false</item>
        <item name="hintEnabled">false</item>
    </style>

    <style name="TextInputEditText" parent="BaseMinAccesibilityElement">
        <item name="android:layout_gravity">center</item>
        <item name="color">@color/gray_text</item>
        <item name="android:textSize">16dp</item>
        <item name="android:textColorHint">@color/blue_text</item>
    </style>

</resources>